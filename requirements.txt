# Core ADK framework (brings in google-genai and all Cloud dependencies)
google-adk==1.33.0
google-genai==1.75.0

# LiteLLM + Anthropic — optional, only needed if SYNTHESIS_MODEL=anthropic/claude-*
litellm==1.83.14
anthropic==0.102.0

# Financial data
yfinance==1.3.0
pandas==3.0.3

# FRED macroeconomic data
requests==2.34.1

# News / RSS
feedparser==6.0.12

# Config & env
PyYAML==6.0.3
python-dotenv==1.2.2

# Database (SQLite is bundled with Python — no extra install needed)
# Web dashboard
streamlit>=1.35.0
plotly>=5.0.0

# Tests
pytest>=8.0.0
